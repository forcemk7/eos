/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // File upload size limits are handled in API route handlers
  // For App Router API routes, body size is handled by the runtime
}

module.exports = nextConfig
